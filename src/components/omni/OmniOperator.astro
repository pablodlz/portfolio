---
import Sec from './Sec.astro';
import Deep from './Deep.astro';
import { OPERATOR_FNS, OPERATOR_NOTE } from '../../lib/omni';

const detailed = OPERATOR_FNS.filter((f) => f.deep.length > 0);
---

<Sec
  n="04"
  id="operador"
  title="Achar o bug é metade. A outra metade é conduzir a caça."
  lede="Os motores acham. O resto — julgar, escolher, insistir, encadear — é o que normalmente exigiria um caçador sênior humano ao lado. E é medido contra o resultado real, porque reflexão sem dado vira oráculo confiante e errado."
>
  <ol class="fns">
    {OPERATOR_FNS.map((f, i) => (
      <li class="fn">
        <span class="fn-n" aria-hidden="true">{String(i + 1).padStart(2, '0')}</span>
        <div>
          <h3 class="fn-t">{f.title}</h3>
          <p class="fn-d" set:html={f.solves} />
        </div>
      </li>
    ))}
  </ol>

  <Deep summary="O detalhe de cada função">
    {detailed.map((f) => (
      <>
        <h4>{f.title}</h4>
        {f.deep.map((d) => (
          <p set:html={d} />
        ))}
      </>
    ))}
  </Deep>

  <p class="o-note" set:html={OPERATOR_NOTE} />
</Sec>

<style>
  .fns {
    display: grid;
    grid-template-columns: 1fr 1fr;
    column-gap: var(--space-7);
    margin-top: var(--space-6);
    list-style: none;
  }
  .fn {
    display: grid;
    grid-template-columns: 2.5rem minmax(0, 1fr);
    gap: var(--space-3);
    align-items: start;
    padding-block: var(--space-4);
    border-top: 1px solid var(--border);
  }
  .fn-n {
    font-family: var(--font-mono);
    font-size: 1.25rem;
    font-weight: 700;
    line-height: 1.2;
    color: var(--border-strong);
    font-variant-numeric: tabular-nums;
  }
  .fn:hover .fn-n {
    color: var(--accent);
  }
  .fn-t {
    font-size: 1.0625rem;
    letter-spacing: -0.01em;
  }
  .fn-d {
    margin-top: var(--space-2);
    color: var(--text-2);
    font-size: var(--fs-small);
    line-height: 1.75;
  }

  @media (max-width: 820px) {
    .fns {
      grid-template-columns: 1fr;
    }
  }
</style>
