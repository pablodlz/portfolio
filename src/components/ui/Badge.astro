---
interface Props {
  tone?: 'accent' | 'ok' | 'warn' | 'crit' | 'info' | 'neutral';
  dot?: boolean;
  pulse?: boolean;
  class?: string;
}
const { tone = 'neutral', dot = false, pulse = false, class: className } = Astro.props;
---

<span class:list={['badge', `badge--${tone}`, className]}>
  {dot && <span class:list={['badge-dot', { 'badge-dot--pulse': pulse }]} aria-hidden="true"></span>}
  <slot />
</span>

<style>
  .badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 3px 10px;
    border-radius: var(--radius-pill);
    border: 1px solid var(--border);
    background: var(--bg-raised-2);
    color: var(--text-2);
    font-family: var(--font-mono);
    font-size: var(--fs-xs);
    font-weight: 500;
    line-height: 1.6;
    white-space: nowrap;
  }
  /* v3: paleta única — todos os tons de destaque rendem laranja.
     Semântica preservada pela FORMA: ok/accent/info/crit = preenchido,
     warn ("em andamento") = outline. */
  .badge--accent,
  .badge--ok,
  .badge--info,
  .badge--crit {
    background: var(--accent-soft);
    border-color: var(--accent-border);
    color: var(--accent);
  }
  .badge--warn {
    background: transparent;
    border-color: var(--accent-border);
    color: var(--accent);
  }

  .badge-dot {
    width: 6px;
    height: 6px;
    border-radius: var(--radius-pill);
    background: currentColor;
    flex: none;
  }
  .badge-dot--pulse {
    animation: badge-pulse 2s var(--ease) infinite;
  }
  /* halo na cor do próprio badge (ok=verde, accent=laranja…) */
  @keyframes badge-pulse {
    0%,
    100% {
      box-shadow: 0 0 0 0 color-mix(in srgb, currentColor 18%, transparent);
      opacity: 1;
    }
    50% {
      box-shadow: 0 0 0 5px color-mix(in srgb, currentColor 18%, transparent);
      opacity: 0.7;
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .badge-dot--pulse {
      animation: none;
    }
  }
</style>
